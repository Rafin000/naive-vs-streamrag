# Cosine Similarity
Cosine similarity measures how close two vectors point in the same direction, ignoring their
length. It is computed as the dot product of the vectors divided by the product of their
magnitudes, giving a value from -1 to 1. In retrieval, a higher cosine similarity between the
query embedding and a document embedding means the document is more relevant to the query.
