# Embeddings
An embedding maps text to a vector so that semantically similar text lands close together.
Retrieval compares the query embedding against document embeddings using cosine similarity.
OpenAI provides an embeddings API (text-embedding-3-small); it is separate from generation.
